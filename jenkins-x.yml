buildPack: javascript
pipelineConfig:
  pipelines:
    pullRequest:
      promote:
        steps:
        - command: npm run cypress:run -- --env application_url=$(jx get preview --current)
          name: cypress-e2e-tests
