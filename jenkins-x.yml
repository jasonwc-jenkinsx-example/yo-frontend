buildPack: javascript
pipelineConfig:
  pipelines:
    pullRequest:
      promote:
        steps:
        - command: sh scripts/ci/generate_cypress_env.sh
          name: setup-cypress
        - command: npm run cypress:run
          name: cypress-e2e-tests
